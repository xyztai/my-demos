server:
  tomcat:
    uri-encoding: UTF-8
  port: 7024
spring:
  profiles:
    active: dev
  application:
    name: ware24-eht-cache
  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

# mybatis 配置
mybatis:
  type-aliases-package: com.eht.cache.entity
  mapper-locations: classpath:/mapper/*.xml